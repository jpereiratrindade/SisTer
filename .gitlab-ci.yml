# Acrescente este job ao .gitlab-ci.yml do repositório.
sister-pages:
  image: alpine:3.20
  script:
    - test -s site/index.html
  pages:
    publish: site
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
